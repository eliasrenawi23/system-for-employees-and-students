package Protocol;

/**
 * This enum is used in the message protocol that is sent between client and server
 * this is used as a simple return of success or failure.
 * */
public enum MsgReturnType {

	Success, Failure
}
